# 安全、資料倫理與限制

## AI 的使用範圍

- LLM 只用於一處：規劃器的「情境理解」（`ai_agent.parse_scenario`）。它只能從封閉選項挑選（縣市、鄉鎮、災害類型、災情類別、回報者身分），不能捏造地名或新增選項；任何失敗退回關鍵字規則（`scenario_rules`）。
- 平台生成（composer）、同地點多人回報成案、嚴重度分級、案件狀態機、隱私轉換，全部是確定性程式，不經模型。
- 民眾通報內容與個資**不會**送入模型。

## 隱私

- 公開 API（`/v1/public/*`）不輸出姓名、電話、Email、IP、精確座標或門牌：座標粗化至約 110 公尺、地址保留路段／村里、自由文字中的電話／Email／身分證字號／「我是某某」會被遮蔽。
- 回報者身分只以加鹽雜湊（`REPORTER_HASH_SALT`）保存於 `reports.reporter_key`，僅用於計算不同回報者。
- 內部 API 包含個資（處理單位需要聯繫），務必設定 `ADMIN_API_KEY`。
- 照片會公開顯示於案件頁；表單已提醒民眾避免拍到人臉與車牌，但系統不做自動模糊。

## 存取控制

- 設定 `ADMIN_API_KEY` 後，除 `/v1/public/*`、`/v1/health`、文件頁外，所有端點需 `X-API-Key`。Demo 模式（未設定）全部開放。
- 目前沒有使用者帳號、角色或操作者身分驗證；`actor_name` 為自填欄位，供稽核參考。
- `DEMO_MODE=true` 會開放 `/v1/demo/nantou` 示範資料產生器，正式環境請關閉。

## 官方資料

- 只介接有文件的公開 API：水利署水利資料開放平台、內政部消防署避難收容處所點位檔、南投縣政府 CKAN DataStore、中央氣象署開放資料（需金鑰）、NCDR CAP（需會員授權的 feed URL）。
- 憑證或上游不可用時，圖層回報 `disabled` / `unavailable`，不以假資料替代。
- 水位警戒狀態由站況資料的警戒水位推算；雨量分級依氣象署大雨／豪雨門檻；皆為輔助判讀，以主管機關公告為準。
- 部分政府網站憑證缺少 Subject Key Identifier，`connectors/base.py` 關閉 OpenSSL strict profile 但保留鏈與主機名驗證。

## 已知限制

- 鄉鎮歸屬以最近鄉鎮中心點推定，非權威地理編碼；鄉鎮中心點目前只內建南投縣。
- 聚類以平面 bbox 預篩＋haversine 判距；未考慮道路拓撲（河對岸同距離仍可能被聚在一起）。
- 案件編號序號以平台內計數產生，重試可處理偶發衝突，但非嚴格連號。
- 通知（LINE）為盡力而為：未設定憑證時記錄為模擬；失敗不影響案件狀態。
- 本系統是輔助工具，不取代官方災害應變指揮、警報發布與撤離命令。

## 第二波官方資料的限制

- 土石流潛勢溪流／崩塌潛勢區為水保署年度圖資，非即時；警戒為即時 JSON，兩者以編號對應，新編號溪流若尚未入圖資則以鄉鎮中心標示。
- 水庫與計畫停電沒有公開座標，地圖位置為鄉鎮示意（屬性 `indicative: true`）；台電僅開放計畫性停電，事故停電不在開放資料內。
- 路況消息（TDX）沒有座標，只以文字比對縣市／鄉鎮後列表顯示；CCTV 截圖由 TDX 提供之 URL 直接載入。
- 雷達回波需 `CWA_API_KEY`；影像由後端代理並快取，回放幀數以實際收到的觀測為限，不補插。
- 淹水潛勢圖（水利署）僅提供 7z 大檔（南投 156 MB，2018 年第三代），本輪未導入。
